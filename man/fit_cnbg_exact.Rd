% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnbg.R
\name{fit_cnbg_exact}
\alias{fit_cnbg_exact}
\title{Fit the exact frailty-augmented CNBG posterior (rstan)}
\usage{
fit_cnbg_exact(triangle, prior = "ref", alpha = 3, kappa_cap = 1000, ...)
}
\arguments{
\item{triangle}{Incremental paid-loss triangle (matrix); future cells \code{NA}.}

\item{prior}{\code{"ref"}, \code{"empb"}, or \code{c(shape, rate)}.}

\item{alpha}{Severity shape, held fixed.}

\item{kappa_cap}{Upper cap applied to \code{kappa} draws.}

\item{...}{Passed to the sampler (\code{chains}, \code{iter}, \code{warmup}, \code{seed},
\code{adapt_delta}, ...).}
}
\value{
A \code{cnbg_fit} (\code{method = "exact"}).
}
\description{
Fits the exact, frailty-augmented Bayesian posterior of Remark 5.12 by
Hamiltonian Monte Carlo. Conditional on the accident-year frailty, each cell
is compound Poisson-Gamma (Tweedie, \eqn{1<p<2}); the density is evaluated by
a truncated latent-count summation, numerically equivalent to the Dunn and
Smyth (2005) series. The mean structure is the same Stage-1 free-row GLM used
by \code{\link[=fit_cnbg]{fit_cnbg()}}; the severity shape \code{alpha} is held fixed (paid triangles do
not identify it). This is the single-frailty (\eqn{\kappa_c\to\infty}) model:
one row frailty \eqn{\kappa_r} per accident year, no cell shock.
}
\details{
The returned object is a \code{cnbg_fit} with \code{method = "exact"}, carrying the
same \code{(kappa, A)} posterior summaries and \code{kappa_draws}/\code{A_draws} as the
working-likelihood engine, so \code{\link[=reserve_cnbg]{reserve_cnbg()}} and \code{\link[=diagnose_kappa]{diagnose_kappa()}} apply
unchanged. The exact engine is a theoretical reference: on \eqn{10\times10}
triangles the \eqn{\kappa}-\eqn{\lambda} funnel makes HMC fragile, and the
working-likelihood \code{\link[=fit_cnbg]{fit_cnbg()}} is the recommended default for deployment.
}
\examples{
\dontrun{
tri <- simulate_cnbg(kappa = 5, alpha = 3)$triangle
fit <- fit_cnbg_exact(tri, prior = "ref", alpha = 3)
fit
reserve_cnbg(fit)
diagnose_kappa(fit)
}
}
