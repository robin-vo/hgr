% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnbg.R
\name{reserve_cnbg}
\alias{reserve_cnbg}
\title{Posterior predictive reserve distribution (CNBG-Bayes)}
\usage{
reserve_cnbg(fit, B = 4000L, propagate_mean = FALSE, level = 0.95)
}
\arguments{
\item{fit}{A \code{cnbg_fit} from \code{\link[=fit_cnbg]{fit_cnbg()}} with \code{method = "bayes"}.}

\item{B}{Number of predictive draws.}

\item{propagate_mean}{Propagate Stage-1 mean-coefficient uncertainty.}

\item{level}{Predictive interval level.}
}
\value{
An object of class \code{cnbg_reserve}.
}
\description{
Conditioning-respecting posterior predictive reserve: the observed triangle
is held fixed, accident-year frailties are drawn from their conjugate
posterior, future cells are drawn from the CNBG conditional moments, and
parameter uncertainty in \code{(kappa, A)} is integrated over the posterior.
}
\details{
Setting \code{propagate_mean = TRUE} additionally propagates estimation
uncertainty in the Stage-1 mean coefficients by drawing them from their
asymptotic distribution; no data are resampled, so this remains distinct
from the refit-on-pseudo-triangle bootstrap. The default \code{FALSE} is the
strictly conditional predictive of the paper's Algorithm.
}
\examples{
set.seed(1)
tri <- simulate_cnbg(kappa = 5, alpha = 3)$triangle
reserve_cnbg(fit_cnbg(tri, "bayes"))
}
