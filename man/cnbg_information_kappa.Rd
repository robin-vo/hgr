% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnbg.R
\name{cnbg_information_kappa}
\alias{cnbg_information_kappa}
\title{Information bound on kappa from the amount triangle}
\usage{
cnbg_information_kappa(
  kappa_grid = c(3, 5, 10, 20, 50),
  alpha = 3,
  I = 10L,
  J = 10L,
  Y_bar = 10000,
  mu_log_base = 7,
  mu_growth = 0.05,
  w_ratio = 0.7,
  prior = .cnbg_prior_ref,
  kappa_trunc = 0.1
)
}
\arguments{
\item{kappa_grid}{True \code{kappa} values.}

\item{alpha}{Severity shape.}

\item{I, J, Y_bar, mu_log_base, mu_growth, w_ratio}{Calibration (see
\code{\link[=simulate_cnbg]{simulate_cnbg()}}).}

\item{prior}{Length-2 \code{c(shape, rate)} kappa prior; default reference.}

\item{kappa_trunc}{Lower truncation used by the prior (Stan-style \code{>= 0.1}).}
}
\value{
A data frame: \code{kappa}, \code{I_diag}, \code{Delta}, \code{G}, \code{I_pi},
\code{prior_frac}, \code{vantrees_sd}.
}
\description{
Computes, at a given calibration, the working-independence (diagonal) Fisher
information for \code{kappa}, the within-row score-covariance correction, the
Godambe information \code{G}, the prior information \code{I_pi} (van Trees), the prior
fraction and the van Trees standard-deviation floor on \code{kappa}, across a
grid of true \code{kappa}. Reproduces the paper's van Trees table.
}
\details{
\code{I_pi} depends only on the (truncated) prior and is independent of the
data calibration.
}
\examples{
cnbg_information_kappa(kappa_grid = c(3, 10, 50), alpha = 3)
}
