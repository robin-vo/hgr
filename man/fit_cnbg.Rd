% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnbg.R
\name{fit_cnbg}
\alias{fit_cnbg}
\title{Fit the CNBG model to a paid-loss triangle}
\usage{
fit_cnbg(
  triangle,
  method = c("bayes", "wls"),
  prior = "ref",
  kappa_cap = 1000,
  adjust = TRUE
)
}
\arguments{
\item{triangle}{Incremental paid-loss triangle (matrix); observed cells
non-\code{NA}, future cells \code{NA}.}

\item{method}{\code{"bayes"} (posterior over \code{(kappa, A)}) or \code{"wls"} (point
estimate).}

\item{prior}{\code{"ref"} (Gamma(2, 0.4)) or \code{"empb"} (Gamma(1.78, 0.22)), or a
length-2 numeric \code{c(shape, rate)}.}

\item{kappa_cap}{WLS boundary cap on \code{kappa}.}

\item{adjust}{For \code{method = "bayes"}, apply the cluster-robust Godambe
curvature adjustment (default \code{TRUE}). \code{FALSE} returns the naive
working-likelihood posterior, which is overconfident and should be used
only for comparison.}
}
\value{
An object of class \code{cnbg_fit}.
}
\description{
Estimates the mean structure by a free-row quasi-Poisson GLM (Chain-Ladder
means) and the structural pair \code{(kappa, A)} either by the Bayesian
working-likelihood posterior (\code{method = "bayes"}, the recommended default)
or by the two-stage weighted least-squares point estimate
(\code{method = "wls"}).
}
\note{
The Bayesian engine is a deterministic grid quadrature of the
working-likelihood posterior of \code{(kappa, A)} (target \code{z ~ Gamma(2, 2/mu_z)},
\code{mu_z = A nu + nu^2 / kappa}). That working likelihood treats the cells of
an accident-year row as independent, but they share a row frailty, so the
naive posterior manufactures information and its credible intervals are
several-fold too narrow (they badly undercover the true \code{kappa}). With
\code{adjust = TRUE} the curvature is rescaled to the cluster-robust Godambe
information (rows as independent clusters) via the magnitude adjustment of
Ribatet, Cooley and Davison (2012); the reported \code{widen} factor is how much
the \code{kappa} posterior SD is inflated relative to the naive fit. Even
adjusted, amount triangles identify \code{kappa} only weakly, so the posterior
is largely prior-governed -- treat point summaries with caution and lean on
\code{\link[=diagnose_kappa]{diagnose_kappa()}}.
}
\examples{
set.seed(1)
tri <- simulate_cnbg(kappa = 5, alpha = 3)$triangle
fit <- fit_cnbg(tri, method = "bayes")
fit
}
