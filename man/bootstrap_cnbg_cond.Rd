% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnbg.R
\name{bootstrap_cnbg_cond}
\alias{bootstrap_cnbg_cond}
\title{Conditioning-respecting point-estimate bootstrap (WLS-cond)}
\usage{
bootstrap_cnbg_cond(
  triangle,
  B = 4000L,
  propagate_mean = FALSE,
  level = 0.95,
  kappa_cap = 1000
)
}
\arguments{
\item{triangle}{Incremental paid-loss triangle.}

\item{B}{Number of bootstrap draws.}

\item{propagate_mean}{Propagate Stage-1 mean-coefficient uncertainty
(default \code{FALSE}, matching the point-estimate design).}

\item{level}{Predictive interval level.}

\item{kappa_cap}{WLS boundary cap.}
}
\value{
An object of class \code{cnbg_reserve}.
}
\description{
Point-estimate counterpart to \code{\link[=reserve_cnbg]{reserve_cnbg()}}: identical predictive engine
(shared accident-year frailty, conjugate update, CNBG conditional cell
draws) but with \code{(kappa, A)} fixed at the WLS point estimate instead of
integrated over the posterior. Because the two share the engine, the gap
between them isolates the value of posterior integration over \code{(kappa, A)}.
}
\examples{
set.seed(1)
tri <- simulate_cnbg(kappa = 5, alpha = 3)$triangle
bootstrap_cnbg_cond(tri)
}
